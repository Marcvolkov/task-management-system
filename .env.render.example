# ==================================
# Render.com Environment Configuration
# ==================================

# These environment variables are specifically for Render deployment.
# Most of these will be set automatically by Render or via render.yaml

# ==================================
# Automatically Set by Render
# ==================================
# DATABASE_URL=postgresql://user:pass@host:port/db?sslmode=require
# PORT=10000
# RENDER_EXTERNAL_URL=https://your-app-name.onrender.com

# ==================================
# Set via render.yaml
# ==================================
NODE_ENV=production
JWT_SECRET=auto_generated_by_render
JWT_EXPIRE=7d
FRONTEND_URL=${RENDER_EXTERNAL_URL}
ALLOWED_ORIGINS=${RENDER_EXTERNAL_URL}

# ==================================
# Optional Manual Configuration
# ==================================
# Add these manually in Render dashboard if needed

# Database Pool Configuration (optional)
# DB_POOL_MAX=20
# DB_IDLE_TIMEOUT=30000
# DB_CONNECTION_TIMEOUT=2000

# Additional CORS Origins (if you have multiple domains)
# ALLOWED_ORIGINS=https://your-app.onrender.com,https://yourdomain.com

# Application Metadata (optional)
# REACT_APP_NAME=Task Management System
# REACT_APP_VERSION=1.0.0

# Logging Level (optional)
# LOG_LEVEL=info

# ==================================
# Render Deployment Checklist
# ==================================
# ✅ render.yaml file exists in repository root
# ✅ GitHub repository connected to Render
# ✅ PostgreSQL database created and connected
# ✅ Build command: npm install && npm run build
# ✅ Start command: npm start
# ✅ Health check path: /api
# ✅ Auto-deploy enabled for main branch
# ✅ Environment variables configured
# ✅ Database tables created (run init.sql manually)

# ==================================
# Render-Specific Notes
# ==================================
# 1. DATABASE_URL includes SSL by default
# 2. PORT is always 10000 on Render
# 3. RENDER_EXTERNAL_URL is your app's public URL
# 4. JWT_SECRET should be generated by Render for security
# 5. Health checks hit /api endpoint every 30 seconds
# 6. Logs are available in Render dashboard
# 7. Auto-deploy triggers on git push to main branch