# ==================================
# Task Management System - Root Environment Configuration
# ==================================

# This is the root .env.example file for the monolith deployment.
# For development, you may want to use separate .env files in client/ and server/ directories.
# For production monolith deployment, you can use this root .env file.

# ==================================
# Server Configuration
# ==================================
NODE_ENV=production
PORT=5000

# Database Configuration
# Use DATABASE_URL for production (recommended for cloud deployments)
DATABASE_URL=postgresql://username:password@host:port/database

# Alternative: Individual database parameters (for local development)
# DB_HOST=localhost
# DB_PORT=5432
# DB_NAME=task_management
# DB_USER=your_db_username
# DB_PASSWORD=your_db_password

# JWT Configuration
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production
JWT_EXPIRE=7d

# CORS Configuration
# In monolith deployment, frontend and backend share the same domain
FRONTEND_URL=https://yourapp.example.com
ALLOWED_ORIGINS=https://yourapp.example.com,https://yourdomain.com

# Database Initialization (optional)
INIT_SECRET=your_secret_init_key_for_database_setup

# ==================================
# Client Configuration (Monolith)
# ==================================
# In monolith deployment, API calls use relative paths
# No REACT_APP_API_URL needed - will default to relative '/api' paths

# Optional: Application metadata
REACT_APP_NAME=Task Management System
REACT_APP_VERSION=1.0.0

# ==================================
# Deployment Platform Variables
# ==================================
# These are typically set by your deployment platform

# Heroku
# HEROKU_APP_NAME=your-app-name

# Render
# RENDER_EXTERNAL_URL=https://yourapp.onrender.com

# Railway
# RAILWAY_PROJECT_NAME=your-project-name

# ==================================
# Security Checklist for Production:
# ==================================
# ✅ Change JWT_SECRET to a strong random string (use: openssl rand -base64 32)
# ✅ Set NODE_ENV=production
# ✅ Use DATABASE_URL for database connection
# ✅ Restrict ALLOWED_ORIGINS to your actual domains only
# ✅ Never commit this file with real secrets
# ✅ Use platform-specific secret management in production